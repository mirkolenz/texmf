\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{@font-fira}

\ifluatexorxetex
	\setmainfont[
		BoldFont = * SemiBold,
		BoldItalicFont = * SemiBold Italic,
	]{Fira Sans}

	\setsansfont[
		BoldFont = * SemiBold,
		BoldItalicFont = * SemiBold Italic
	]{Fira Sans}

	\setmonofont{Fira Mono}

	\if@overleaf
		\setmathfont{Asana-Math.otf}
	\else
		\setmathfont{Fira Math}

		\setmathfont[range = \setminus]{Asana-Math.otf}
	\fi
\else
	\RequirePackage[sfdefault,lining,semibold]{FiraSans}
	\RequirePackage{FiraMono}
	\RequirePackage{newtxsf}
\fi

\if@koma
	\recalctypearea
\fi
